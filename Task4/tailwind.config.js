/** @type {import('tailwindcss').Config} */
module.exports = {
      content: [
        "./src/**/*.{html,ts}", // Adjust if your templates are in different locations
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    }

